# ===================================================
# Marine Science Lab Equipment Scheduler
# Environment Configuration
# ===================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ===================================================

# Server
PORT=8080
SESSION_SECRET=change-this-to-a-long-random-string

# Allowed email domain for registration (without @)
ALLOWED_DOMAIN=uvi.edu

# Admin emails (comma-separated, these users get admin role)
ADMIN_EMAILS=admin@uvi.edu,labmanager@uvi.edu

# SMTP settings for sending verification emails
# For Gmail: use smtp.gmail.com, port 587, and an App Password
# For SendGrid: use smtp.sendgrid.net, port 587, user "apikey", pass = your API key
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM="Marine Science Lab <noreply@uvi.edu>"

# Base URL of your deployed app (used in verification email links)
# For local dev: http://localhost:8080
# For App Engine: https://your-project.appspot.com
BASE_URL=http://localhost:8080
